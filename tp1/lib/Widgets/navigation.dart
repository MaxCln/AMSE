/*import 'package:flutter/material.dart';

import '../models/media.dart';

import '../meal.dart';

class Navigation {
  void toMeal(BuildContext ctx, Meal meal) {
    Navigator.of(ctx).push(
      MaterialPageRoute(
        builder: (_) {
          return MealPage(meal);
        },
      ),
    );
  }

  // Prototype de retour en arrière à une page précise en supprimant l'arbre des routes (par exemple une fois la commande terminée)
  /*void backTo(BuildContext ctx, Widget page) {
    Navigator.of(ctx).pushAndRemoveUntil(
      MaterialPageRoute<dynamic>(
        builder: (BuildContext ctx) => page,
      ),
      (route) => *indiquer ici jusqu'ou supprimer*,
    );
  }*/
}*/
